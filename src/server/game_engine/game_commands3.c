#include		<server/game.h>

int			game_expulse(t_event *e)
{
  return (EXIT_SUCCESS);
}

int			game_fork(t_event *e)
{
  return (EXIT_SUCCESS);
}

int			game_broadcast(t_event *e)
{
  return (EXIT_SUCCESS);
}
